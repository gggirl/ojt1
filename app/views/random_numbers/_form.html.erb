<%= form_for(@random_number) do |f| %>
  <% if @random_number.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@random_number.errors.count, "error") %> prohibited this random_number from being saved:</h2>

      <ul>
      <% @random_number.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Name %><br />
    <%= f.text_field :Name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
