<h1>Listing user_msgs</h1>

<table>
  <tr>
    <th>Message</th>
    <th>Chatiw</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_msgs.each do |user_msg| %>
  <tr>
    <td><%= user_msg.message %></td>
    <td><%= user_msg.Chatiw %></td>
    <td><%= link_to 'Show', user_msg %></td>
    <td><%= link_to 'Edit', edit_user_msg_path(user_msg) %></td>
    <td><%= link_to 'Destroy', user_msg, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User msg', new_user_msg_path %>
