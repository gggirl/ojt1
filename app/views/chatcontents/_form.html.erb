<%= form_for(@chatcontent) do |f| %>
  <% if @chatcontent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatcontent.errors.count, "error") %> prohibited this chatcontent from being saved:</h2>

      <ul>
      <% @chatcontent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
