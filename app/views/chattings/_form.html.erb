<%= form_for(@chatting) do |f| %>
  <% if @chatting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatting.errors.count, "error") %> prohibited this chatting from being saved:</h2>

      <ul>
      <% @chatting.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% end %>
