<%= form_for(@chat_room) do |f| %>
  <% if @chat_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chat_room.errors.count, "error") %> prohibited this chat_room from being saved:</h2>

      <ul>
      <% @chat_room.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 
  
   <div class="field">
   
    <%= f.text_field :message %>
  
     <%= f.submit  value="Send" %>
    <!--<%= link_to '<button type="button">Send</button>'.html_safe, new_chat_room_path %>-->
  </div>

<% end %>

